# frontend/nginx.conf
server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # This line is critical for React Router:
        # If a file isn't found, serve index.html instead.
        try_files $uri $uri/ /index.html;
    }
}